const SelectTemplateProvider = () => {
  return {
    name: 'template-provider',
    message: 'select a provider',
    type: 'list',
    choices: ['default'],
  }
}

export default SelectTemplateProvider
